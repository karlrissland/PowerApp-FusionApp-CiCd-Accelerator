name: Release

on:
  workflow_dispatch:
  
permissions:
  id-token: write
  contents: read

jobs:
  deployment_matrix:
    strategy:
      matrix:
         environment: ["Test","Prod"]
    uses: ./.github/workflows/deploy-to-environment.yml
    with:
      environment: ${{ matrix.environment }}
    
    
